# Core dependencies
pymupdf>=1.27.0
pymupdf4llm>=0.3.0

# OCR for scanned PDFs — Tesseract installed via apt-get in Dockerfile
pytesseract>=0.3.10
opencv-python-headless>=4.8.0
pyyaml>=6.0
python-dotenv>=1.0.0

# HTTP client for API calls
httpx>=0.27.0
requests>=2.32.0  # Required for OpenRouter client (synchronous requests)
tenacity>=8.5.0  # Retry logic for API calls with exponential backoff

# FastAPI backend
fastapi>=0.115.0
uvicorn[standard]>=0.30.0
python-multipart>=0.0.9
websockets>=12.0
pydantic>=2.0.0  # Data validation (required by FastAPI)

# Word document generation
python-docx>=1.1.0

# Excel export
openpyxl>=3.1.0

# Database
sqlalchemy>=2.0.0
psycopg[binary]>=3.1.0
alembic>=1.13.0

# Auth
passlib[bcrypt]>=1.7.4
python-jose[cryptography]>=3.3.0

# Testing
pytest>=8.0.0
pytest-cov>=4.1.0
pytest-mock>=3.12.0

# Type checking (optional, for development)
mypy>=1.8.0

# Code quality (optional, for development)
black>=24.0.0
flake8>=7.0.0
isort>=5.13.0
